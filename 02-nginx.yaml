- name: install and start nginx
  hosts: frontend
  become: yes
  tasks:
  - name: install mginx
    ansible.builtin.package:
      name: nginx
      state: present

  - name: start nginx
    ansible.builtin.package:
      name: nginx
      state: started
      enabled: yes
    
    
 
 