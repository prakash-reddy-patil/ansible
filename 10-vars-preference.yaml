- name: varible preference
  hosts: local
  connection: local
  # vars:
  #   NAME: from play level
  # vars_files:
  # - course.yaml
  # vars_prompt:
  # - name: NAME
  #   prompt: What is NAME?
  #   private: false
  tasks:
    - name: testing varibles preference
      # vars:
      #   NAME: from task level
      ansible.builtin.debug:
        msg: "NAME: {{ NAME }}"


# 1) first preference is for arguments or command line
# 2) prompt
# 3) vars_files
# 4) prompt
# 5) varible